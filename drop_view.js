const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { await prisma.$executeRawUnsafe('DROP VIEW IF EXISTS curriculum;'); } run().finally(() => prisma.$disconnect());
